# Tutorial REST

To build and run the application perform:

    mvn clean install cargo:run
    
To verify whether the application is running one can open    
## Swagger documentation

When the server is running 
## Notes

To display the specification as generated by Swagger got to: http://localhost:8080/rest/api/swagger.json

